import { TouchableOpacity, Text, Fab, Stack, VStack} from "native-base";

export function ButtonFloat () {
    return(
        <Stack fill center>
            <VStack width={20} height={30}>
                
            </VStack>
        </Stack>
    );
}